release:
  stage: release
  image: python:latest
  script:
    - pip3 install gitlab-release gitlab-release-notes-generator
    - Output=$(gitlab-release-notes-generator --private-token $RELEASE_CLI)
    - gitlab-release --description "${Output}" --zip "release-${CI_COMMIT_TAG}.zip" *
  only:
    - tags
